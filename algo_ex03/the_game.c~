#include "brightcode.h"

void	dec_to_hex(void)
{
  int	p;
  char	base[] = "0123456789ABCDEF";

  p = 0;
  while (p / nb < 9)
    p = p * 10;
  while (p > 0)
    {
      bc_write_char(base[(p / nb) % 10]);
      p = p / 10;
    }
}
